name: First Workflow
on: push
jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Print compiler version
              run: echo "Hello World"
            - name: clean exit
              run: echo "exiting"